'use strict';

/**
 * car-subcategory controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::car-subcategory.car-subcategory');
